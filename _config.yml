# Permalinks
#
# Use of `relative_permalinks` ensures post links from the index work properly.
permalink:           pretty
relative_permalinks: false
paginate:            5
gems: [jekyll-paginate]
paginate_path: "page:num"

# Setup
title:               Coconet
tagline:             "NADE generative model of music, with new insights on sampling"
description:         "samples for different musical scenarios"
base-url:             "base-url"
baseurl:             ""
url:                 "https://coconets.github.io"
github:              "https://github.com/coconets"
timezone:            UTC

# Custom vars
version:             0.0.1

# Build settings
markdown: kramdown

theme: jekyll-theme-modernist
