# Permalinks
#
# Use of `relative_permalinks` ensures post links from the index work properly.
permalink:           pretty
relative_permalinks: false
paginate:            5
gems: [jekyll-paginate]
paginate_path: "page:num"

# Setup
title:               Coconet
tagline:             "composing music with orderless NADE and Gibbs sampling"
description:         "counterpoint by convolution: a deep generative model for partial score completion"
base-url:             "base-url"
baseurl:             ""
url:                 "https://coconets.github.io"
github:              "https://github.com/coconets"
timezone:            UTC

# Custom vars
version:             0.0.1

# Build settings
markdown: kramdown

theme: jekyll-theme-modernist
